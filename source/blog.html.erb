---
pageable: true
per_page: 25
title: Writing
---
<section id="writing-list">
  <h1>Writing</h1>
  <p>My thoughts here and on social media are my own and not
    those of my employer or other organizations I participate in.</p>

  <% page_articles.each_with_index do |article, i| %>
    <ul>
      <li>
        <h2><%= link_to article.title, article %></h2>
        <div class="article-details">
          <div class="article-date">
            <%= article.date.strftime('%Y-%m-%d') %>
          </div>
          <!--ul class="tags">
            <% article.tags.each do |tag| %>
              <%= link_to tag, tag_path(tag) %>
            <% end %>
          </ul-->
        </div>
      </li>
    </ul>
  <% end %>

  <% if paginate && (prev_page || next_page) %>
    <p>Page <%= page_number %> of <%= num_pages %></p>
    <p>
    <% if prev_page %>
      <%= link_to '<i class="fa fa-angle-double-left"></i> previous',
          prev_page, :class => "pagination" %>
    <% end %>
    <% if next_page %>
      <%= link_to 'next <i class="fa fa-angle-double-right"></i>',
          next_page, :class => "pagination" %>
    <% end %>
    </p>
  <% end %>
</section>
